# L08 示例数据目录

当前第 8 章的企业知识库数据写在 `practice/agent_mode_common.py` 中，便于课堂直接阅读代码。

后续可以把以下内容迁移到本目录：

- `knowledge_bases.json`：按业务域拆分的示例文档。
- `eval_cases.json`：企业知识库问答评测集。
- `run_traces/`：多 Agent 运行 trace 和审核记录。
- `audit_logs/`：脱敏后的路由、检索和引用审计日志。
