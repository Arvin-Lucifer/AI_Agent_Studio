# L11 运行数据

本目录保存第 11 章脚本生成的评测和监控结果。

当前脚本会生成：

- `eval_dataset.json`：Agent 评测数据集。
- `eval_results.json`：自动化评测结果。
- `cost_summary.json`：成本监控示例摘要。

这些文件可以删除后重新生成：

```bash
cd <course-root>/lessons/L11_eval_and_deployment
bash practice/preclass_run.sh
```
